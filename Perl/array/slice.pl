#!/usr/bin/env perl

# 取列表中index=0、1和3的元素，
# 列表上下文中返回所取得元素的列表，赋值给数组
my @arr = (11, 22, 33, 44)[0,1,3];  # 11 22 44

# 数组切片, 提取多个特定成员, 这里最后一个成员为 undefined, 打印为空
my @arr = @arr[1,1,2,3];  # 22 22 44
print "@arr" . "\n";

# 打印最后两个成员
print "@arr[$#arr-1..$#arr]" . "\n";
